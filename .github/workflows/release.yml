name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing tag to release (example v0.1.0)
        required: true
        type: string

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build release (${{ matrix.target }})
    runs-on: ${{ matrix.runner }}
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-amd64-glibc
            runner: ubuntu-24.04
            libc: glibc
            cross: ""
          - target: linux-arm64
            runner: ubuntu-24.04-arm
            libc: glibc
            cross: ""
          - target: linux-i386
            runner: ubuntu-24.04
            libc: glibc
            cross: zig
          - target: linux-armv7
            runner: ubuntu-24.04
            libc: glibc
            cross: zig
          - target: linux-armv6
            runner: ubuntu-24.04
            libc: glibc
            cross: zig
          - target: darwin-arm64
            runner: macos-15
            libc: ""
            cross: ""
          - target: darwin-amd64
            runner: macos-15-intel
            libc: ""
            cross: ""
          - target: windows-amd64
            runner: ubuntu-24.04
            libc: ""
            cross: zig
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}

      - name: Load CI config
        run: sh ci/scripts/load-ci-env.sh

      - name: Install Linux deps
        if: runner.os == 'Linux' && matrix.libc != 'musl'
        run: sh ci/scripts/install-deps.sh

      - name: Install Odin
        if: matrix.libc != 'musl'
        run: sh ci/scripts/install-odin.sh

      - name: Setup Zig
        if: matrix.cross == 'zig'
        run: sh ci/scripts/setup-zig.sh

      - name: Fetch Reticulum-Go
        if: matrix.libc != 'musl'
        run: sh ci/scripts/fetch-rns.sh

      - name: Install Go (for librns)
        if: matrix.libc != 'musl'
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.26.x"

      - name: Build glibc host / cross
        if: matrix.libc != 'musl' && matrix.target != 'linux-amd64-glibc'
        env:
          TARGET: ${{ matrix.target }}
        run: |
          set -eu
          if [ -z "${RNS_ROOT:-}" ]; then
            echo "RNS_ROOT missing after fetch-rns" >&2
            exit 1
          fi
          TARGET="${TARGET}" RNS_ROOT="${RNS_ROOT}" sh ci/scripts/build-target.sh

      - name: Build linux-amd64 glibc
        if: matrix.target == 'linux-amd64-glibc'
        run: |
          set -eu
          if [ -z "${RNS_ROOT:-}" ]; then
            echo "RNS_ROOT missing after fetch-rns" >&2
            exit 1
          fi
          make vendor-librns RNS_ROOT="${RNS_ROOT}"
          make dist LIBC=glibc

      - name: Package
        run: |
          set -eu
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ inputs.tag }}"
          else
            TAG="${{ github.ref_name }}"
          fi
          STAGE="dist/${APP_NAME}-${TAG}-${{ matrix.target }}"
          mkdir -p "${STAGE}" dist
          case "${{ matrix.target }}" in
          windows-*)
            if ! command -v zip >/dev/null 2>&1; then
              sudo apt-get update
              sudo apt-get install -y --no-install-recommends zip
            fi
            cp -a bin/ren-tui.exe bin/ren-listen.exe bin/librns.dll "${STAGE}/"
            cp -a LICENSE README.md "${STAGE}/"
            (
              cd dist
              zip -r "${APP_NAME}-${TAG}-${{ matrix.target }}.zip" "$(basename "${STAGE}")"
              sha256sum "${APP_NAME}-${TAG}-${{ matrix.target }}.zip" >"${APP_NAME}-${TAG}-${{ matrix.target }}.zip.sha256"
            )
            ;;
          *)
            LIB=
            if [ -f bin/librns.so ]; then LIB=bin/librns.so
            elif [ -f bin/librns.dylib ]; then LIB=bin/librns.dylib
            fi
            cp -a bin/ren-tui bin/ren-listen ${LIB} "${STAGE}/"
            cp -a LICENSE README.md "${STAGE}/"
            (
              cd dist
              tar -czf "${APP_NAME}-${TAG}-${{ matrix.target }}.tar.gz" "$(basename "${STAGE}")"
              sha256sum "${APP_NAME}-${TAG}-${{ matrix.target }}.tar.gz" >"${APP_NAME}-${TAG}-${{ matrix.target }}.tar.gz.sha256"
            )
            ;;
          esac
          ls -la dist

      - name: Upload dist
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-${{ matrix.target }}
          path: dist/*
          if-no-files-found: error
          retention-days: 30

  windows-smoke:
    name: Windows smoke
    needs: build
    if: always() && !cancelled()
    continue-on-error: true
    runs-on: windows-2025
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit
      - name: Download windows artifact
        id: dl
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-windows-amd64
          path: dist

      - name: Skip when windows build missing
        if: steps.dl.outcome != 'success'
        shell: bash
        run: echo "windows artifact missing, skipping smoke"

      - name: Smoke
        if: steps.dl.outcome == 'success'
        shell: bash
        run: |
          set -eu
          ZIP=$(ls dist/*.zip | head -n 1)
          mkdir -p smoke
          unzip -q "${ZIP}" -d smoke
          DIR=$(find smoke -mindepth 1 -maxdepth 1 -type d | head -n 1)
          cd "${DIR}"
          ./ren-tui.exe --version
          ./ren-tui.exe --paths
          ./ren-listen.exe --version

  publish:
    name: Publish immutable release
    needs: [build, windows-smoke]
    if: always() && !cancelled()
    runs-on: ubuntu-24.04
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}

      - name: Load CI config
        run: sh ci/scripts/load-ci-env.sh

      - name: Resolve tag
        id: meta
        run: |
          set -eu
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "tag=${{ inputs.tag }}" >>"${GITHUB_OUTPUT}"
          else
            echo "tag=${{ github.ref_name }}" >>"${GITHUB_OUTPUT}"
          fi

      - name: Download artifacts
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: artifacts
          pattern: release-*
          merge-multiple: true

      - name: Create draft release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.meta.outputs.tag }}
        run: |
          set -eu
          if gh release view "${TAG}" >/dev/null 2>&1; then
            STATE=$(gh release view "${TAG}" --json isDraft --jq .isDraft)
            if [ "${STATE}" != "true" ]; then
              echo "release ${TAG} already published (immutable releases cannot add assets)" >&2
              exit 1
            fi
          else
            gh release create "${TAG}" \
              --draft \
              --generate-notes \
              --title "${APP_NAME} ${TAG}" \
              --repo "${{ github.repository }}"
          fi

      - name: Upload assets then publish
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.meta.outputs.tag }}
        run: |
          set -eu
          shopt -s nullglob
          set -- artifacts/*.tar.gz artifacts/*.zip artifacts/*.sha256
          if [ "$#" -eq 0 ]; then
            echo "no release assets found (all matrix targets failed)" >&2
            exit 1
          fi
          echo "publishing $# asset files from successful targets"
          ls -la artifacts || true
          gh release upload "${TAG}" "$@" --clobber --repo "${{ github.repository }}"
          gh release edit "${TAG}" --draft=false --repo "${{ github.repository }}"
          gh release view "${TAG}" --repo "${{ github.repository }}"
